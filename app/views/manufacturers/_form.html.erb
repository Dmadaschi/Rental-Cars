<%= render partial: 'errors', locals: { record: @manufacturer } %>
<%= form_with model: @manufacturer, local: true do |form| %>
  <%= form.label :name%>
  <%= form.text_field :name%>
  <%= form.submit 'Enviar'%> 
<%end%>
